// OPEN_MODAL
export const openModal = (boolTrue) => ({
    type: 'OPEN_MODAL',
    boolTrue
});


// CLOSE_MODAL
export const closeModal = (boolFalse) => ({
    type: 'CLOSE_MODAL',
    boolFalse
});